{
  "name": "serre",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Serre AI — formal foundations of AI reasoning",
  "workspaces": [
    "orchestrator",
    "cli",
    "site",
    "forge"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=orchestrator",
    "build": "npm run build --workspace=orchestrator && npm run build --workspace=cli",
    "forge": "npm run start --workspace=cli --"
  }
}
