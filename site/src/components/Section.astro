---
interface Props {
  class?: string;
  border?: boolean;
}

const { class: className = '', border = false } = Astro.props;
---

<section class:list={[
  'max-w-6xl mx-auto px-6 section-gap',
  border && 'border-b border-[var(--color-border)]',
  className,
]}>
  <slot />
</section>
