---
import { statusColor } from '../utils/helpers';

interface Props {
  status: string;
}

const { status } = Astro.props;
---

<span class={`font-mono text-[10px] px-2 py-0.5 border shrink-0 ${statusColor(status)}`}>
  {status.toUpperCase()}
</span>
