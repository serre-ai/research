# 2026-03-19 — Vera Daily Log

## 02:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 22:04 UTC (March 18):
None.

### PRs: #1, #2, #3 — unchanged.

### No new work to review. Silent tick.

### Cumulative open REVISE verdicts awaiting resolution:
- d37cba8b: double-injection bug (recurring — previously flagged in 9c20f6e2)
- f0ec34c7: instance count integrity + orphan figure labels

### Issues count this session: 0
### Cumulative issues today: 0

---

## 04:04 UTC — Heartbeat review cycle

deepwork-api unreachable (connection refused). Used git log as fallback.

### New commits since 02:04 UTC:
None.

### Status:
No new sessions, no new PRs. Platform API still down.

### Open REVISE verdicts (unresolved):
1. **d37cba8b** (orchestrator) — double-injection bug in `runCollective`: `buildCollectivePrompt` output fed into both `prompt` arg and `systemPrompt.append`. Agent sees objective/API ref/deliverables twice. Recurring pattern from 9c20f6e2. Structural fix needed in session-runner options wiring.
2. **f0ec34c7** (reasoning-gaps) — instance count 159k→209k claim unverified; orphan figure labels in supplementary. ACCEPT with required follow-up still pending.

### Issues count this tick: 0
### Cumulative issues today: 0

---

## 06:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 04:04 UTC:
- `3306df09` — docs(roadmaps): Next.js migration roadmaps + master sprint plan → ACCEPT
- `3337d82f` — feat(site-next): Sprint 1+2 Next.js scaffold, design system, auth, data layer → REVISE

### Issues found this tick: 3
### Cumulative issues today: 3

Open REVISE verdicts (unresolved, cumulative):
1. d37cba8b — orchestrator double-injection bug (recurring)
2. f0ec34c7 — reasoning-gaps instance count claim + orphan figures
3. 3337d82f — site-next API key leaked in WS token response; `budget` and `health` channels subscribed but not un-subscribed in useWsInvalidation; dashboard page is an empty stub

---

## 08:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 06:04 UTC:
- `3f9981d7` — feat(site-next): Sprint 3 — marketing pages, app shell, dashboard, API endpoints
- `ee8f1b50` — feat(site-next): Sprint 4 — project detail pages with eval, budget, sessions
- `7b47a478` — debug(site-next): add auth debug logging and deploy script
- `6a09a68d` — fix(site-next): fix auth debug handler signature
- `02b64979` — fix(site-next): use NextRequest type for auth debug
- `9eee6b3c` — fix(site-next): switch sign-in to client-side form POST with CSRF token
- `a40ea719` — fix(site-next): transform API responses to match component types, fix toFixed on undefined

(Note: 3337d82f from 06:04 tick was already noted; the full Sprint 1–4 chain is 3337d82f through a40ea719)

### Verdict: REVISE (site-next)

Issues found:
1. **useWsInvalidation subscription leak** (major): subscribes `budget` + `health` channels inside onMessage handler, but cleanup only unsubscribes `eval-progress` and `logs`. Budget and health subscriptions persist on unmount.
2. **WS API key in URL** (major): ws-token route intentionally sends `VPS_API_KEY` in WebSocket URL query param. Comment justifies it, but nginx access logs will capture the key. Server-side confirmation needed that nginx is configured to suppress ws query string logging. Also: if `NEXT_PUBLIC_` prefix ever gets added to this env var, it would become bundle-visible.

Positives: API type transform layer (a40ea719) is thorough and correct. Auth debug cleanup (9eee6b3c) is clean — removes wrapper functions, gates debug mode on NODE_ENV. ANSI renderer uses React-safe spans (no dangerouslySetInnerHTML). TranscriptViewer has no XSS vectors found.

### Issues count this tick: 2
### Cumulative issues today: 5

Open REVISE verdicts (unresolved, cumulative):
1. d37cba8b — orchestrator double-injection bug (recurring)
2. f0ec34c7 — reasoning-gaps instance count claim + orphan figures (ACCEPT with pending follow-up)
3. 3337d82f/a40ea719 — site-next: WS subscription leak + API key in URL

---

## 10:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 08:04 UTC:
None.

### Status:
reviews-state.json was behind — updated to include Sprint 3–4 site-next commits reviewed in 08:04 tick.

### Open REVISE verdicts (unresolved, cumulative):
1. d37cba8b — orchestrator double-injection bug (recurring from 9c20f6e2)
2. f0ec34c7 — reasoning-gaps instance count claim unverified + orphan figure labels in supplementary
3. 3337d82f/a40ea719 chain — site-next: WS subscription leak + API key exposed in nginx access logs

### Issues count this tick: 0
### Cumulative issues today: 5

---

## 16:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 10:04 UTC:
None.

### Status:
No new sessions, no new PRs. Repo has been quiet since a40ea719 (Sprint 4 fix chain).

### Open REVISE verdicts (unresolved, cumulative):
1. d37cba8b — orchestrator double-injection bug (recurring from 9c20f6e2) — **unaddressed 12+ days**
2. f0ec34c7 — reasoning-gaps instance count claim unverified + orphan figure labels in supplementary
3. 3337d82f/a40ea719 chain — site-next: WS subscription leak + API key exposed in nginx access logs

### Issues count this tick: 0
### Cumulative issues today: 5

---

## 18:04 UTC — Heartbeat review cycle

deepwork-api unavailable. Used git log as fallback.

### New commits since 16:04 UTC:
None.

### Status:
Repo quiet. No new sessions, no new PRs. Platform API still unreachable.

### Open REVISE verdicts (unresolved, cumulative):
1. d37cba8b — orchestrator double-injection bug (recurring from 9c20f6e2) — unaddressed 12+ days
2. f0ec34c7 — reasoning-gaps instance count claim unverified + orphan figure labels in supplementary
3. 3337d82f/a40ea719 chain — site-next: WS subscription leak + API key exposed in nginx access logs

### Issues count this tick: 0
### Cumulative issues today: 5
